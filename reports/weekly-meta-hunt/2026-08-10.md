# Weekly Meta Hunt — 2026-08-10

## Run blocked: target decks not found in this repo

This week's scheduled run was configured to research and cross-reference
findings against two decks:

- **Teal Mask Ogerpon Box** — expected as `DECK_OGERPON_BOX` in `src/engine/decks.py`
- **Crustle-based wall/stall deck** — expected somewhere in the same file

Neither exists. Checked:

- `src/engine/decks.py` on `main` — the `DECKS` registry only contains:
  `dragapult`, `charizard_xy`, `raging_bolt`, `gardevoir`, `colorless`, `fire`,
  `fighting`, `dark`, `metal`, `water`, `greninja`, `beedrill`. No Ogerpon or
  Crustle entry, under any name.
- Git history on `main` (`git log --all --grep`) — no commit ever mentions
  Ogerpon or Crustle.
- All 17 remote branches (`core-stabilization`, `feat/piece3-target-policies`,
  `feature/beedrill`, `feature/best-deck-view`, `feature/deck-importer`,
  `feature/fighting-tune`, `feature/gardevoir-tune`, `feature/grass-meta-fire`,
  `feature/learning-pipeline`, `feature/matrix-extras`, `feature/matrix-mcts`,
  `feature/mega-greninja`, `feature/more-cards`, `feature/more-decks`,
  `feature/round-robin`, `feature/web-ui`) — none define these decks either.
- `decks/imported/` (where TCG Live pastes land via `cli.py --import-deck`) is
  gitignored and empty in this checkout — no imported recipe to fall back on.

Since the whole point of this routine is cross-referencing live meta results
against the *actual* 60-card lists the user plays, and no such lists exist
anywhere in this repo, running the research pass would mean either (a)
fabricating placeholder decklists to hang the findings on, or (b) writing
"proposed adjustments" for decks that don't exist — both violate this
project's stated goal of honesty over spin, and the task's explicit
"never fabricate a decklist" rule. So this run stops here instead of
guessing.

**No web research, community-chatter pass, or forecast pass was run this
week** — there was nothing concrete to cross-reference it against yet.

## What's needed before next week's run can proceed

One of:
1. The real Teal Mask Ogerpon Box and Crustle wall/stall 60-card recipes get
   added to `src/engine/decks.py` (or imported via `cli.py --import-deck` and
   committed somewhere durable, since `decks/imported/` is currently
   gitignored), or
2. The scheduled task prompt gets pointed at whichever decks *do* exist in
   this repo today, or
3. Confirmation that these two decks live in a different repo/branch this
   session doesn't have access to (this session is scoped to
   `goatnuts1/ptcg-analyzer` only).

A push notification was sent flagging this blocker.
