# viz/

Standalone visual tools. Not part of the core pipeline — open directly in a browser.

- `data_explorer.html` — interactive card-data inspector. Shows how raw
  pokemontcg.io JSON maps to a rendered card, annotates every field in plain
  language, and includes a teaching-toy "next best move" demo. Cards embedded
  are real samples from the pool.
