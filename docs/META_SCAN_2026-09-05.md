# Meta scan — 2026-09-05

Baseline: `docs/META_SCAN_2026-08-27.md` (which itself carried forward
2026-08-20 — the live table had been blocked for two scans running). This is
the first fetch since **World Championships 2026 (Aug 28–30)** produced
results.

## 1. Live fetch succeeded this time

`play.limitlesstcg.com/decks?format=standard&rotation=2026&set=PBL` returned
data — no egress block this session. **Caveat: only the top 12 rows were
captured** (this WebFetch summarizes rather than paginates the raw table; the
08-20 baseline had ~25 rows totaling near 100% share, this pull only sums to
~62.8%). Treat coverage below as a floor, not the full field, until a scan can
pull the remaining rows.

| rank | archetype (Limitless name) | share % | real WR % | registry match |
|---|---|---|---|---|
| 1 | Dragapult | 7.55 | 53.53 | `dragapult` |
| 2 | Mega Excadrill | 7.35 | 48.71 | `mega_excadrill` (user's deck) |
| 3 | Festival Lead | 5.84 | 50.77 | `festival_lead` |
| 4 | Alakazam Dudunsparce | 5.81 | 52.84 | `alakazam_deck` (name likely updated by Limitless — flagged, not confirmed identical list) |
| 5 | Dragapult Blaziken | 5.74 | 52.94 | `dragapult_blaziken` |
| 6 | Dragapult Dusknoir | 5.60 | 50.19 | `dragapult` (same list — this is a sub-row of the Dusknoir variant Justin Newdorf's list already covers) |
| 7 | Slowking | 5.49 | 52.22 | `slowking` / `slowking_annihilape` / `slowking_slowbro` |
| 8 | **N's Zoroark** | 5.19 | 48.01 | **NONE — see below** |
| 9 | Grimmsnarl Froslass | 4.15 | 51.06 | `grimmsnarl_froslass` |
| 10 | Dhelmise | 3.96 | 48.36 | `hide_n_sneak` (the Ghost box the user is piloting on ladder right now) |
| 11 | Other | 3.63 | 37.93 | n/a (catch-all) |
| 12 | Toucannon | 2.51 | 47.35 | `toucannon` |

Registry coverage of this visible slice: **10 of 12 rows already matched**,
54.0 of the 62.8 visible share points. The two real gaps are "Other" (not a
real archetype) and N's Zoroark.

## 2. The one real build-bar crosser: N's Zoroark

**5.19% share, 48.01% real WR — clears the ≥2% build bar cleanly, with no
registry deck for it.** Checked `docs/LADDER_LOG.md` for a Zoroark
build-bar trigger from ladder play: none found (no logged game against or
with an N's Zoroark ex list since the log's 2026-08 entries). This is a pure
live-table find, not a ladder-corroborated one — still clears the bar on
share alone.

**Not built this scan.** Fetching a real decklist, verifying every card's
text against limitlesstcg/Bulbapedia (never memory — the deck runs Night
Joker, a copy-whatever's-in-discard attack whose damage genuinely varies
turn to turn, which is exactly the kind of mechanic that's easy to get wrong
from memory), implementing effects, registering the deck, and running it
through the matrix is the "expensive step" the skill flags — a multi-agent
build on the scale of the Meta-2026-08 batch (3 archetypes) or
`dragapult_worlds`, not a same-scan add. Flagging it here as the queued next
build rather than starting it unasked mid-scan.

## 3. What changed for `mega_excadrill` (the user's deck)

Real WR is now directly visible for the first time: **48.71%**, versus the
previously-cited aggregate 49.51% from `docs/META_GAUNTLET_2026-08.md` — the
two numbers corroborate each other closely (0.8pt apart, different
measurement windows). No new bad matchup surfaced; the standing two flagged
cells (fighting 15%, grimmsnarl_froslass 46.7%, per the 08-17 overnight
matrix) are unchanged and this scan has no new evidence on either.

## 4. What changed for `hide_n_sneak` (the Ghost box, the user's other deck)

This is the deck behind Limitless's "Dhelmise" row — the user has been
piloting it live on ladder (see the 2026-09-01 through 2026-09-05 LADDER_LOG
entries: Ghost box now 2-0 in that stretch). Real WR **48.36%**, share 3.96%
— both numbers are now independently confirmed by a live tournament source,
not just our own sim + ladder anecdote. No registry or effect changes needed.

## 5. SHARE_TRENDS rolled (partial)

Rolled forward for every archetype visible in this scan's top-12 capture
(`src/analysis/futures.py`): `dragapult` (12.47→13.15, combining the
Dragapult + Dusknoir rows per existing convention), `mega_excadrill`
(7.84→7.35), `festival_lead` (6.55→5.84), `alakazam_deck` (5.24→5.81),
`dragapult_blaziken` (6.02→5.74), `slowking` (5.36→5.49),
`grimmsnarl_froslass` (4.55→4.15), `hide_n_sneak` (4.08→3.96), `toucannon`
(3.18→2.51). `raging_bolt`, `cynthia_garchomp`, `fighting`, `greninja`,
`beedrill` were NOT in the visible top-12 this scan (below ~2.5% or folded
into "Other") — left unchanged rather than guessed. `python3 cli.py
--futures` and the full test suite (99 files) both run clean against the
update.

## 6. Stoppers table — not re-measured

No new decks were built this scan (nothing crossed the bar into an actual
build), so `docs/matrix_2026-08_mcts2.json` is untouched and the stoppers
table from 08-20/08-27 still stands as the most current matrix-derived read.
Standard caveats apply in full: ~13pt aggro bias, `festival_lead` /
slowking-toolbox cells are pilot floors not ratings.

## 7. Horizon

No change to the 08-27 horizon note — 30th Celebration (Sept 16, 2026, low
expected impact) and Delta Reign (Nov 6, 2026, Mega Rayquaza ex + three
unverified Megas) stand as written there. Bulbapedia/limitlesstcg card-page
fetches were not attempted this scan (budget went to the live meta table and
this report); still pending direct-text confirmation for the three
non-Rayquaza Megas.

## Recommendation for next scan

1. Retry the live fetch with an approach that captures the full table (not
   just the top 12) so representable-share can be recomputed honestly.
2. **N's Zoroark is the queued build** — real WR/share now on record twice
   (this scan) with no ladder corroboration yet; if it appears on ladder
   against `Goatest1` before the next scan, that's a second independent
   trigger.
3. Retry Bulbapedia/limitlesstcg fetches for the three unverified Storm
   Emeralda Megas (Golisopod/Golurk/Malamar) — still flags, not facts, two
   scans running.
