# generated data artifacts — rebuild with the fetch script
data/*.json
# ...except the hand-maintained supplement of Standard-legal cards the upstream
# GitHub dump hasn't published yet (newer Mega-era cards). Tracked + merged by
# fetch_standard_pool.py so a re-fetch reproduces the full pool deterministically.
!data/manual_cards.json
docs/last_build.log
build.log

# python
*.pyc
__pycache__/
.venv/
.pytest_cache/

# os
.DS_Store

# export bundles / snapshots
*.zip
*.tar.gz

# saved game replays (generated by `cli.py --save-game`) — reproducible from seed
saved_games/
optimizer_runs/
